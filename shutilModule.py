# this module is used to perform highlevel file operations
import shutil
import os
# shutil.copy("shutil.py","shutil2.py")
shutil.copytree("day27","newfol") # used to copy intire folder
shutil.rmtree("newfolmu")